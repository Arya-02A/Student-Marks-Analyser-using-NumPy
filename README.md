# Student-Marks-Analyser-using-NumPy
- A basic NumPy project to analyze marks of students across different subjects. It calculates total, average, highest, and lowest marks for each student.
- Python Concepts Used :- NumPy arrays
                        - NumPy functions: np.sum(), np.mean(), np.max(), np.min()
                        - Array indexing and slicing
